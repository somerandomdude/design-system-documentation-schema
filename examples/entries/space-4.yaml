id: space-4
kind: token
name: Space 4
description: A single step on the base spacing scale - 4 times the 4px base unit.

tokenType: spacing
source: ./tokens.dtcg.json

metadata:
  status: {status: stable}
  since: 1.0.0
  tags: [spacing]
  group: space

sections:
  - kind: guidelines
    for: all
    items:
      - statement: Prefer this over an arbitrary pixel value in component styles.
        level: should
