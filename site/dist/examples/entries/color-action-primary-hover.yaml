id: color.action.primary.hover
kind: token
name: Action Primary Hover
description: The fill color for a high-emphasis interactive surface while the pointer is over it.

tokenType: color

metadata:
  status: {status: stable}
  since: 1.0.0
  tags: [color, action, primary, hover]
  group: color.action

refs:
  - to: color.action.primary
    rel: relates-to
  - href: ./tokens.dtcg.json
    rel: file
    role: color.action.primary.hover

sections:
  - kind: guidelines
    for: all
    items:
      - statement: Apply only on pointer-hover; use a separate token for the pressed/active state.
        level: should
