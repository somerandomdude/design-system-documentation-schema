{
  "$schema": "../../../schema/dsds.bundled.schema.json",
  "dsdsVersion": "0.1",
  "entity": {
    "kind": "component",
    "name": "button",
    "displayName": "Button",
    "metadata": [
      {
        "kind": "description",
        "value": "An interactive element that triggers an action when activated. Buttons communicate what will happen when the user interacts with them."
      },
      {
        "kind": "status",
        "status": "stable",
        "platformStatus": {
          "react": { "status": "stable", "since": "1.0.0" },
          "ios": { "status": "stable", "since": "2.0.0" }
        }
      },
      {
        "kind": "since",
        "value": "1.0.0"
      },
      {
        "kind": "tags",
        "items": ["action", "form", "interactive"]
      }
    ],
    "documentBlocks": [
      {
        "kind": "purpose",
        "useCases": [
          {
            "description": "When the user needs to trigger an action such as submitting a form, saving data, or confirming a decision.",
            "kind": "positive"
          },
          {
            "description": "When the action navigates the user to a different page or URL.",
            "kind": "negative",
            "alternative": {
              "name": "link",
              "rationale": "Links carry native navigation semantics. Screen readers announce them as links, and browsers support open-in-new-tab."
            }
          }
        ]
      },
      {
        "kind": "guideline",
        "items": [
          {
            "guidance": "Limit each surface to one primary button.",
            "rationale": "Multiple primary buttons dilute visual hierarchy. When everything is emphasized, nothing is.",
            "kind": "required",
            "category": "visual-design"
          },
          {
            "guidance": "Use a verb or verb phrase that describes the action the button performs.",
            "rationale": "Action-oriented labels set clear expectations about what will happen on activation.",
            "kind": "required",
            "category": "content"
          }
        ]
      }
    ],
    "agents": {
      "intent": "Trigger a user-initiated action within the current view without causing page navigation.",
      "constraints": [
        { "rule": "Do not use for navigating to a different page or URL.", "level": "must-not" },
        { "rule": "Limit each surface to one primary-emphasis button.", "level": "must" }
      ],
      "disambiguation": [
        { "entity": "link", "distinction": "Use button for in-page actions; use link for navigation to a URL." }
      ],
      "keywords": ["action", "submit", "click", "CTA", "trigger"]
    }
  }
}
