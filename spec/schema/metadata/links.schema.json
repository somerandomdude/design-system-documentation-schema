{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://designsystemdocspec.org/v0.13.0/metadata/links.schema.json",
  "title": "DSDS Links Metadata Field",
  "description": "Links to external resources — source code, design files, docs pages, packages. For inter-entity references, use the entity's `relationships` array instead.",
  "$defs": {
    "links": {
      "type": "array",
      "items": { "$ref": "../common/link.schema.json#/$defs/link" },
      "minItems": 1,
      "description": "Links to external resources. Examples: source code, design files, docs pages, packages. Expressing how entities relate through links is deprecated (DSDS-010) — use the entity's `relationships` array for typed edges."
    }
  }
}
