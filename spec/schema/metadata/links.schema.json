{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://designsystemdocspec.org/v0.15.2/metadata/links.schema.json",
  "title": "DSDS Links Metadata Field",
  "description": "Links to external resources — source code, design files, docs pages, packages. For inter-entity references, use the entity's `relationships` array instead.",
  "$defs": {
    "links": {
      "type": "array",
      "items": { "$ref": "../common/link.schema.json#/$defs/link" },
      "minItems": 1,
      "description": "Links to external resources. Examples: source code, design files, docs pages, packages. Links cannot express how entities relate — the entity's `relationships` array holds those typed edges."
    }
  }
}
