{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://designsystemdocspec.org/v0.6/metadata/thumbnail.schema.json",
  "title": "DSDS Thumbnail Metadata",
  "description": "A visual thumbnail for compact display contexts — a single image, carried directly as a URL plus required alt text.",
  "$defs": {
    "thumbnailMetadata": {
      "type": "object",
      "required": ["kind", "url", "alt"],
      "properties": {
        "kind": { "type": "string", "const": "thumbnail", "description": "Identifies this metadata entry as a thumbnail." },
        "url": { "type": "string", "format": "uri", "description": "URL or relative path to the thumbnail image." },
        "alt": { "type": "string", "description": "Alternative text describing the thumbnail image. MUST be provided for accessibility." }
      },
      "additionalProperties": false
    }
  }
}
