{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://designsystemdocspec.org/v0.7/metadata/thumbnail.schema.json",
  "title": "DSDS Thumbnail Metadata Field",
  "description": "A visual thumbnail for compact display contexts — a single image carried as a URL plus required alt text.",
  "$defs": {
    "thumbnail": {
      "type": "object",
      "description": "A visual thumbnail for compact display contexts — a single image carried as a URL plus required alt text.",
      "required": ["url", "alt"],
      "properties": {
        "url": {
          "type": "string",
          "format": "uri",
          "description": "URL or relative path to the thumbnail image."
        },
        "alt": {
          "type": "string",
          "description": "Alternative text describing the thumbnail image. MUST be provided for accessibility."
        }
      },
      "additionalProperties": false
    }
  }
}
