{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://designsystemdocspec.org/v0.15.1/metadata/thumbnail.schema.json",
  "title": "DSDS Thumbnail Metadata Field",
  "description": "A single image thumbnail for compact display, as a URL plus required alt text.",
  "$defs": {
    "thumbnail": {
      "type": "object",
      "description": "A single image thumbnail for compact display, as a URL plus required alt text.",
      "required": ["url", "alt"],
      "properties": {
        "url": {
          "$ref": "../common/presentation.schema.json#/$defs/mediaUrl",
          "description": "URL or relative path to the thumbnail image."
        },
        "alt": {
          "$ref": "../common/presentation.schema.json#/$defs/mediaAlt",
          "description": "Alt text for the thumbnail image. MUST NOT be empty."
        }
      },
      "additionalProperties": false
    }
  }
}
