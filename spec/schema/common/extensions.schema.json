{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://designsystemdocspec.org/v0.1/extensions.schema.json",
  "title": "DSDS Extensions",
  "description": "Vendor-specific extensions for DSDS documents. An open object for tool-specific metadata outside the core schema. Keys MUST use vendor-specific namespaces.",
  "$defs": {
    "extensions": {
      "type": "object",
      "description": "Vendor-specific extensions. Keys MUST use vendor-specific namespaces (reverse domain name notation recommended). Tools that do not recognize an extension MUST preserve it. Extension data SHOULD NOT duplicate core schema fields.",
      "additionalProperties": true
    }
  }
}
