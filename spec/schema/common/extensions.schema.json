{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://designsystemdocspec.org/v0.1/extensions.schema.json",
  "title": "DSDS Extensions Definition",
  "description": "Vendor-specific extensions for DSDS documents. Provides an open object for tool-specific metadata that does not belong in the core schema. Keys MUST use vendor-specific namespaces.",
  "$defs": {
    "extensions": {
      "type": "object",
      "description": "Vendor-specific extensions. Keys MUST use vendor-specific namespaces (reverse domain name notation recommended). Tools that do not recognize an extension MUST preserve it. Extension data SHOULD NOT duplicate information available in core schema fields.",
      "additionalProperties": true
    }
  }
}
