{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://designsystemdocspec.org/v0.9.0/common/entity-ref.schema.json",
  "title": "DSDS Entity Reference Definitions",
  "description": "The one way to point at another entity. `entityRef` is the standalone reference object used wherever a position refers to an entity and nothing else (a pattern's components, a use case's alternative). The leaf definitions (`entityIdentifier`, `entityRole`) are shared by `link`, whose identifier-bearing form is an entity reference with the same semantics — link cannot compose `entityRef` structurally because URL-only links exist, so the sharing happens at the leaves. Tools resolve every entity reference through the same rule: the identifier MUST match a documented entity.",
  "$defs": {
    "entityIdentifier": {
      "type": "string",
      "description": "The identifier of a documented DSDS entity (e.g., 'button', 'form-field', 'color-text-primary'). MUST match that entity's `identifier`. Tools SHOULD resolve it against the entity catalog to build navigable cross-references; a reference that resolves to nothing is a defect in the document."
    },
    "entityRole": {
      "type": "string",
      "description": "The role the referenced entity plays in this context (e.g., 'Carries the inline error', 'Submit, disabled while invalid'). When omitted, the reference is a general association."
    },
    "entityRef": {
      "type": "object",
      "description": "A reference to another entity by identifier, with an optional role describing what the entity does in this context. Used where a position points at an entity and nothing else; for pointers that may also carry a URL or external-resource kind, use `link`.",
      "required": ["identifier"],
      "properties": {
        "identifier": {
          "$ref": "#/$defs/entityIdentifier"
        },
        "role": {
          "$ref": "#/$defs/entityRole"
        }
      },
      "additionalProperties": false
    }
  }
}
