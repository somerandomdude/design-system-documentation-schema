{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://designsystemdocspec.org/v0.1/metadata.schema.json",
  "title": "DSDS Metadata Definition",
  "description": "System-level metadata about the design system — its name, version, organization, URL, and license.",
  "$defs": {
    "metadata": {
      "type": "object",
      "description": "System-level metadata about the design system.",
      "required": ["systemName"],
      "properties": {
        "systemName": {
          "type": "string",
          "description": "The name of the design system."
        },
        "systemVersion": {
          "type": "string",
          "description": "The version of the design system."
        },
        "organization": {
          "type": "string",
          "description": "The organization that maintains the system."
        },
        "url": {
          "type": "string",
          "format": "uri",
          "description": "URL to the system's documentation site."
        },
        "license": {
          "type": "string",
          "description": "SPDX license identifier or license URL."
        }
      },
      "additionalProperties": false
    }
  }
}
