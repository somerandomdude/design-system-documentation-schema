{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://designsystemdocspec.org/v0.1/usecase.schema.json",
  "title": "DSDS Use Case Definitions",
  "description": "Definitions for documenting when to use and when not to use a design system artifact or option. Use cases provide scenario-driven guidance that complements best practices by framing decisions in terms of concrete situations rather than abstract rules. The useCase and useCases defs defined here carry no guideline `type` discriminator — they are the raw data model used for nesting inside variant values, state entries, and as the content of the purpose guideline type.",
  "$defs": {
    "useCase": {
      "type": "object",
      "description": "A single use case scenario describing a situation in which an artifact or option is or is not appropriate.",
      "required": ["description"],
      "properties": {
        "description": {
          "$ref": "rich-text.schema.json#/$defs/richText",
          "description": "A description of the scenario. Should be concrete and situation-driven — describe the user's situation, not the artifact's features."
        },
        "kind": {
          "type": "string",
          "enum": ["positive", "negative"],
          "description": "Describes whether the use case is an example to follow or to not follow."
        },
        "alternative": {
          "type": "object",
          "description": "The recommended alternative artifact for this scenario. Typically provided for whenNotToUse entries to direct the reader toward a more appropriate component, pattern, or token.",
          "required": ["name"],
          "properties": {
            "name": {
              "type": "string",
              "description": "The name of the recommended alternative artifact (e.g., 'link', 'radio-group', 'icon-button', 'empty-state')."
            },
            "rationale": {
              "$ref": "rich-text.schema.json#/$defs/richText",
              "description": "An explanation of why the alternative is more appropriate for this scenario. Cite semantic differences, accessibility implications, or UX rationale when available."
            }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    }
  }
}
