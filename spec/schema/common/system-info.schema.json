{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://designsystemdocspec.org/v0.7.1/common/system-info.schema.json",
  "title": "DSDS System Info Definition",
  "description": "System-level identity and provenance for the design system — name, version, organization, URL, and license. This is the system's identity (its equivalent of an entity's identifier and name), distinct from the descriptive `metadata` array carried by entities.",
  "$defs": {
    "systemInfo": {
      "type": "object",
      "description": "System-level identity and provenance for the design system.",
      "required": ["systemName"],
      "properties": {
        "systemName": {
          "type": "string",
          "description": "The name of the design system."
        },
        "systemVersion": {
          "type": "string",
          "description": "The version of the design system."
        },
        "organization": {
          "type": "string",
          "description": "The organization that maintains the system."
        },
        "url": {
          "type": "string",
          "format": "uri",
          "description": "URL to the system's documentation site."
        },
        "license": {
          "type": "string",
          "description": "SPDX license identifier or license URL."
        }
      },
      "additionalProperties": false
    }
  }
}
