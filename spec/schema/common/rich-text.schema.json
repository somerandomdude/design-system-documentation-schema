{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://designsystemdocspec.org/v0.14.0/common/rich-text.schema.json",
  "title": "DSDS Rich Text Definition",
  "description": "A markdown text field. Used across all DSDS schema modules for human-written prose.",
  "$defs": {
    "richText": {
      "type": "string",
      "description": "Human-written prose, read as CommonMark markdown (0.27 minimum). No format flag is needed. Plain sentences and inline HTML are both valid markdown, so write plain text, markdown, or embedded HTML as the content needs. Tools MUST render the value as markdown.",
      "minLength": 1
    }
  }
}
