{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://designsystemdocspec.org/v0.7.1/common/status.schema.json",
  "title": "DSDS Status Definition",
  "description": "Shared status vocabulary used by the status metadata entry. `statusValue` is the lowercase kebab-case lifecycle string (draft, experimental, stable, deprecated, plus custom values). `platformStatus` captures one platform's readiness: its lifecycle stage, the version it became available, and an optional deprecation notice. It needs a deprecationNotice when that platform is deprecated.",
  "$defs": {
    "statusValue": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*$",
      "description": "A lifecycle status value. MUST be lowercase kebab-case. Standard values: 'draft' (under development, not ready for use), 'experimental' (available, but the API may change without notice), 'stable' (production-ready; changes follow semver), 'deprecated' (scheduled for removal). Custom values are allowed (e.g., 'sunset', 'archived', 'beta') and MUST follow the same pattern."
    },
    "platformStatus": {
      "type": "object",
      "description": "Readiness of an artifact on one platform. Tracks the lifecycle stage, the version where it became available, and an optional deprecation notice.",
      "required": ["status"],
      "properties": {
        "status": {
          "$ref": "#/$defs/statusValue",
          "description": "Lifecycle status of the artifact on this platform. Uses the same vocabulary as the overall status: 'draft', 'experimental', 'stable', 'deprecated'. Custom values are allowed."
        },
        "since": {
          "type": "string",
          "description": "The version in which this artifact became available on this platform."
        },
        "deprecationNotice": {
          "type": "string",
          "description": "Required when status is 'deprecated'. Says what to use instead on this platform."
        },
        "description": {
          "type": "string",
          "description": "Notes about the artifact's status on this platform (e.g., 'Available as a Web Component wrapper; native implementation planned for v4')."
        }
      },
      "if": {
        "properties": {
          "status": {
            "const": "deprecated"
          }
        }
      },
      "then": {
        "required": ["deprecationNotice"]
      },
      "additionalProperties": false
    }
  }
}
