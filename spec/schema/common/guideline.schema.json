{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://designsystemdocspec.org/v0.1/guideline.schema.json",
  "title": "DSDS Guideline Definitions",
  "description": "A single, unified guideline definition used across all DSDS artifact types. Every guideline is a self-contained, actionable statement paired with a rationale. Optional properties allow guidelines to express external criteria, content targets, and categorization without requiring separate types.",
  "$defs": {
    "guideline": {
      "type": "object",
      "description": "A single, self-contained piece of usage guidance. Guidelines appear in flat arrays on artifact documents. The optional `category` property groups by discipline (e.g., visual-design, accessibility, content). The optional `target` property identifies which anatomy part or content area the guideline applies to. The optional `criteria` property links the guideline to external functional requirements via URLs. The optional `type` property classifies the guideline's enforcement level.",
      "required": ["guidance", "rationale"],
      "properties": {
        "guidance": {
          "$ref": "common.schema.json#/$defs/richText"
        },
        "rationale": {
          "$ref": "common.schema.json#/$defs/richText"
        },
        "type": {
          "type": "string",
          "enum": [
            "required",
            "encouraged",
            "informational",
            "discouraged",
            "prohibited"
          ],
          "description": "The enforcement level of the guideline, classifying how strictly it should be followed. This property answers the question: 'What kind of guideline is this?' Values align with RFC 2119 requirement levels: 'required' (MUST — non-compliance is a defect; use for accessibility mandates, platform constraints, or system invariants), 'encouraged' (SHOULD — follow in most cases; exceptions require justification; use for well-established best practices), 'informational' (MAY — advisory context with no enforcement expectation; use for tips, background, or emerging patterns), 'discouraged' (SHOULD NOT — avoid unless justified; use for practices with known drawbacks), 'prohibited' (MUST NOT — violations are defects; use for patterns that break accessibility, semantics, or system integrity). When omitted, consumers MAY treat the guideline as 'encouraged'."
        },
        "category": {
          "type": "string",
          "description": "The discipline or domain this guideline belongs to. Used to group guidelines by professional concern. Standard values: 'visual-design' (color, typography, spacing, layout), 'interaction' (behavior, animation, transitions, gestures), 'accessibility' (inclusive design, assistive technology, WCAG compliance), 'content' (text, imagery, tone, voice, labeling), 'motion' (animation timing, easing, reduced-motion), 'development' (implementation patterns, performance, code conventions). Custom values are permitted and SHOULD be lowercase kebab-case. Category identifies the discipline; use `tags` for cross-cutting topics that span multiple disciplines."
        },
        "target": {
          "type": "string",
          "description": "The anatomy part or content area this guideline applies to (e.g., 'label', 'icon', 'helper-text'). Typically used when category is 'content'. When omitted, the guideline applies to the artifact as a whole."
        },
        "criteria": {
          "type": "array",
          "items": {
            "type": "string",
            "format": "uri"
          },
          "description": "URLs to external functional requirements or standards that this guideline addresses. Generalizes beyond WCAG to any published criteria — accessibility standards, platform guidelines, regulatory requirements, or internal compliance documents. For WCAG criteria, use the direct URL to the success criterion (e.g., 'https://www.w3.org/TR/WCAG22/#contrast-minimum'). For platform guidelines, link to the specific section (e.g., 'https://developer.apple.com/design/human-interface-guidelines/accessibility')."
        },
        "examples": {
          "type": "array",
          "description": "Examples illustrating the guideline.",
          "items": {
            "$ref": "example.schema.json#/$defs/example"
          }
        },
        "tags": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Freeform keywords to relate guidelines across categories/disciplines (e.g., 'rtl', 'localization', 'validation')."
        }
      },
      "additionalProperties": false
    }
  }
}
