{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://designsystemdocspec.org/v0.7.2/document-blocks/scale.schema.json",
  "title": "DSDS Scale Document Block",
  "description": "Documents an ordered sequence of values that forms a visual scale: a type scale, spacing scale, elevation scale, or other progression of design token values. Each step links to a design token. It may also give a human-readable label, a resolved value for display, and usage notes. Scales set the limited set of values that designers and engineers choose from. They replace ad hoc values with a deliberate, harmonious progression.",
  "$defs": {
    "scaleStep": {
      "type": "object",
      "description": "A single step in an ordered scale. Each step links to a design token that supplies the concrete value for that position in the progression. The optional label, value, and description add context for display without copying the token's authoritative definition.",
      "required": ["token"],
      "properties": {
        "token": {
          "type": "string",
          "description": "The token identifier for this step. MUST reference a token defined in the token schema (e.g., 'space-4', 'font-size-300', 'elevation-low'). This is the single source of truth for the step's value. The optional `value` property is a display convenience only."
        },
        "label": {
          "type": "string",
          "description": "A human-readable label for this step (e.g., 'sm', 'md', 'lg', '2xl', 'body', 'heading-1'). Labels give a shorthand vocabulary that designers and engineers use to talk about scale positions without naming raw values."
        },
        "value": {
          "type": "string",
          "description": "The resolved value for display (e.g., '16px', '1rem', '#1a1a1a', '400'). This is a docs convenience. The authoritative value lives in the referenced token. Tools MAY fill this in from the token catalog."
        },
        "description": {
          "$ref": "../common/rich-text.schema.json#/$defs/richText",
          "description": "Usage notes for this step: when to use it, what it pairs with, and any limits (e.g., 'The base unit. Use for default internal component padding.', 'Reserved for page-level hero sections only.')."
        }
      },
      "additionalProperties": false
    },
    "scale": {
      "type": "object",
      "description": "Documents an ordered scale: a deliberate progression of values that limits design decisions to a harmonious, consistent set. Scales are the main mechanism for spacing systems, type hierarchies, elevation levels, and other graduated visual attributes. The steps array runs from smallest/lowest to largest/highest. Tools SHOULD keep this order for display.",
      "required": ["kind", "identifier", "description", "steps"],
      "properties": {
        "kind": {
          "type": "string",
          "const": "scale",
          "description": "Identifies this document block as a scale spec."
        },
        "identifier": {
          "type": "string",
          "description": "Machine-readable identifier of the scale (e.g., 'spacing-scale', 'type-scale', 'elevation-scale'). Used for cross-referencing and tooling."
        },
        "name": {
          "type": "string",
          "description": "Human-readable name of the scale (e.g., 'Spacing Scale', 'Type Scale', 'Elevation Levels')."
        },
        "description": {
          "$ref": "../common/rich-text.schema.json#/$defs/richText",
          "description": "What this scale is, how it was built (e.g., geometric progression, modular scale), and the rules for using it. Document the scale's rationale and limits: why these values were chosen and when deviation is appropriate."
        },
        "steps": {
          "type": "array",
          "description": "The ordered steps in the scale, from smallest/lowest to largest/highest. Ordering is significant, so tools SHOULD keep it for display. Consumers SHOULD treat adjacent steps as the natural next-smaller and next-larger options when adjusting a value.",
          "items": {
            "$ref": "#/$defs/scaleStep"
          },
          "minItems": 1
        }
      },
      "additionalProperties": false
    }
  }
}
