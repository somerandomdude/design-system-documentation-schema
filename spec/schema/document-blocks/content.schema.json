{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://designsystemdocspec.org/v0.10.2/document-blocks/content.schema.json",
  "title": "DSDS Content Document Block",
  "description": "Specialized, structured content documentation for an artifact or for the design system as a whole. It covers two kinds of reference data. The first is a terminology dictionary of recommended action labels — 'Add', 'Cancel', 'Delete' — with definitions, usage context, and cross-references. The second is localization: RTL behavior, text expansion, pluralization, and other i18n concerns, one entry per concern. At least one of `labels` or `localization` is required. For general content guidance — voice, tone, capitalization rules, with rationale and a conformance level — use a `guidelines` block (category 'content').",
  "$defs": {
    "contentLabelEntry": {
      "type": "object",
      "description": "A single recommended action label or term in the content dictionary. Each entry sets a standard label, its meaning, usage guidance, and cross-references to related or alternative terms. Entries follow the {verb} + {noun} content formula used by systems like Carbon and Gestalt.",
      "required": [
        "term",
        "definition"
      ],
      "properties": {
        "term": {
          "type": "string",
          "description": "The recommended label text (e.g., 'Add', 'Cancel', 'Delete', 'Save as', 'Learn more'). The canonical form as it should appear in the UI."
        },
        "definition": {
          "$ref": "../common/rich-text.schema.json#/$defs/richText",
          "description": "What this label means: the action it represents. Should be concrete and unambiguous (e.g., for Delete: 'Destroys an existing object so that it no longer exists')."
        },
        "usage": {
          "$ref": "../common/rich-text.schema.json#/$defs/richText",
          "description": "When and how to use this label. Include context, formatting rules, and any caveats (e.g., 'Where appropriate, combine Add with the object: Add user, Add role')."
        },
        "alternatives": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Related terms that serve a different purpose but are often confused with this one. Listed for comparison, to help authors pick the right label. The terms SHOULD match other `term` values in the same content guideline so tools can cross-reference them."
        },
        "context": {
          "type": "string",
          "description": "Where this label is used in the UI (e.g., 'Buttons in dialogs', 'Navigation links', 'Form submission', 'Toolbar actions'). Tells authors where the label normally appears."
        },
        "examples": {
          "type": "array",
          "description": "Visual or code examples showing the label in context: do/don't pairs, screenshots of correct usage, or code snippets.",
          "items": {
            "$ref": "../common/example.schema.json#/$defs/example"
          }
        }
      },
      "additionalProperties": false
    },
    "localizationEntry": {
      "type": "object",
      "description": "A single localization or i18n concern. Each entry covers something that affects how content is authored, translated, or rendered across languages and writing systems.",
      "required": [
        "concern",
        "description"
      ],
      "properties": {
        "concern": {
          "type": "string",
          "description": "The localization concern being addressed. Standard values: 'rtl' (right-to-left layout mirroring and bidirectional text), 'text-expansion' (content lengthening during translation), 'pluralization' (plural forms varying across languages), 'date-format' (date and time formatting conventions), 'number-format' (decimal separators, grouping), 'currency' (currency symbol placement and formatting), 'text-direction' (mixed-direction content within a single string), 'icon-direction' (icons that imply directionality), 'truncation' (text overflow behavior across languages), 'sorting' (locale-specific sort order and collation), 'concatenation' (avoiding string concatenation that breaks in other languages). Custom values are allowed and SHOULD be lowercase kebab-case."
        },
        "description": {
          "$ref": "../common/rich-text.schema.json#/$defs/richText",
          "description": "Guidance for handling this localization concern. Should be actionable and specific: say what changes across locales, what the author or developer must do, and what to avoid."
        },
        "examples": {
          "type": "array",
          "description": "Visual or code examples that illustrate the concern: before/after screenshots for RTL, samples of text expansion, or code patterns for pluralization.",
          "items": {
            "$ref": "../common/example.schema.json#/$defs/example"
          }
        }
      },
      "additionalProperties": false
    },
    "content": {
      "type": "object",
      "description": "Specialized, structured content documentation: a terminology dictionary of recommended labels and per-concern localization entries. Each entry is reference data — a fact about the system's vocabulary or its i18n behavior. This block carries no prose rules: general content guidance, with rationale and a conformance level, belongs in a `guidelines` block (category 'content'), and the two blocks complement each other — a guideline says 'Use sentence case' (a rule); a content entry says 'Add: takes an existing object and uses it in a new context' (a reference).",
      "required": [
        "kind"
      ],
      "anyOf": [
        {
          "required": [
            "labels"
          ]
        },
        {
          "required": [
            "localization"
          ]
        }
      ],
      "properties": {
        "kind": {
          "type": "string",
          "const": "content",
          "description": "Identifies this document block as a content spec."
        },
        "description": {
          "$ref": "../common/rich-text.schema.json#/$defs/richText",
          "description": "Prose overview of what this block covers: the scope of the label dictionary and the localization entries. Content philosophy and voice-and-tone rules are general guidance and belong in a `guidelines` block (category 'content'), not here."
        },
        "labels": {
          "type": "array",
          "description": "Recommended action labels and terminology. Each entry sets a standard label, its meaning, usage guidance, and cross-references to related terms. Order matters, so tools SHOULD keep it for display. Authors MAY order alphabetically or by frequency of use.",
          "items": {
            "$ref": "#/$defs/contentLabelEntry"
          },
          "minItems": 1
        },
        "localization": {
          "type": "array",
          "description": "Localization and i18n notes. Each entry covers a concern that affects content across languages and writing systems: RTL mirroring, text expansion during translation, pluralization rules, and other i18n concerns. Order matters, so tools SHOULD display entries in this order. Authors SHOULD order by impact or frequency.",
          "items": {
            "$ref": "#/$defs/localizationEntry"
          },
          "minItems": 1
        }
      },
      "additionalProperties": false
    }
  }
}
