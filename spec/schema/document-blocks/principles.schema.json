{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://designsystemdocspec.org/v0.10.2/document-blocks/principles.schema.json",
  "title": "DSDS Principles Document Block",
  "description": "Documents guiding principles that shape decision-making for a foundation, pattern, or other design system artifact. Each item is a single principle with a short title and a description that explains what it means in practice. Principles answer 'what do we believe?' and 'what limits do we accept?'. They sit above individual `guidelines` entries and shape the approach to a domain (e.g., 'Functional first' for color, 'Use the scale' for spacing).",
  "$defs": {
    "principleEntry": {
      "type": "object",
      "description": "A single guiding principle. Each principle has a short title and a description that explains what it means in practice and how it guides decisions. Principles are high-level. They set direction rather than prescribe specific actions. For specific, actionable rules, use a `guidelines` block instead. For whether to use an artifact at all, use `purpose`.",
      "required": ["title", "description"],
      "properties": {
        "title": {
          "type": "string",
          "description": "A short, memorable name for the principle (e.g., 'Functional first', 'Accessible by default', 'Use the scale', 'Density over decoration'). Should be concise enough to serve as a heading or reference label."
        },
        "description": {
          "$ref": "../common/rich-text.schema.json#/$defs/richText",
          "description": "What this principle means and how it guides decisions. Explain the belief or constraint in concrete terms, not just restate the title. Good descriptions answer: 'What does this look like in practice?' and 'How would I use this to resolve a disagreement?'"
        }
      },
      "additionalProperties": false
    },
    "principles": {
      "type": "object",
      "description": "Documents guiding principles for a foundation or design domain. Use `principles` to answer *what the system believes* and which trade-offs it accepts: the philosophy that shapes a domain's decisions. Principles sit *above* `guidelines`. They set direction, while `guidelines` entries encode the specific rules. A spacing foundation might have 'Use the scale' and 'Density over decoration'. A color foundation might have 'Functional first' and 'Accessible by default'.",
      "required": ["kind", "items"],
      "properties": {
        "kind": {
          "type": "string",
          "const": "principles",
          "description": "Identifies this document block as a principles spec."
        },
        "items": {
          "type": "array",
          "description": "The guiding principles. Each item documents a single principle with a title and description. Ordering is significant, so tools SHOULD display principles in this order. Authors SHOULD order by importance or by the natural progression of the design philosophy.",
          "items": {
            "$ref": "#/$defs/principleEntry"
          },
          "minItems": 1
        }
      },
      "additionalProperties": false
    }
  }
}
