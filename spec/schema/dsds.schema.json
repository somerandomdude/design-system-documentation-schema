{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://designsystemdocspec.org/v0.1/dsds.schema.json",
  "title": "Design System Documentation Standard (DSDS) v0.1",
  "description": "A standard, machine-readable format for design system documentation covering components, design tokens, token groups, themes, styles, and patterns. A DSDS document contains one or more documentation groups — named collections of typed entities. Each entity carries a `type` discriminator that identifies it as a component, token, token-group, theme, style, or pattern. Entity schemas are defined in entities/. Guideline schemas are defined in guidelines/. Common schemas are defined in common/.",
  "type": "object",
  "required": ["dsdsVersion", "documentation"],
  "properties": {
    "$schema": {
      "type": "string",
      "description": "URI reference to the DSDS JSON Schema for validation."
    },
    "dsdsVersion": {
      "type": "string",
      "const": "0.1",
      "description": "The version of the DSDS specification this document conforms to."
    },
    "$profile": {
      "type": "string",
      "enum": ["core", "complete"],
      "default": "complete",
      "description": "The conformance profile this document targets. Profiles control how strictly tools SHOULD validate the document.\n\n- 'core': Minimal viable documentation. Only identity fields are required on entities (type, name for tokens; type, name, displayName for components/styles/patterns/themes). Description, status, and all other properties are optional. Use this profile for early-stage documentation, migration drafts, or skeleton documents that will be filled in over time.\n\n- 'complete': Full specification conformance. All required fields as defined by each entity schema must be present. This is the default when $profile is omitted.\n\nTools SHOULD use this property to adjust validation strictness. When $profile is 'core', tools SHOULD suppress validation errors for missing optional-in-core fields and MAY display a badge or indicator showing the document's completeness level."
    },
    "metadata": {
      "$ref": "common/metadata.schema.json#/$defs/metadata"
    },
    "documentation": {
      "type": "array",
      "description": "One or more documentation groups. Each group is a named collection of typed entities — components, tokens, token groups, themes, styles, and/or patterns. Multiple groups allow a single DSDS file to organize artifacts into logical sections (e.g., one group for foundations, another for components).",
      "items": {
        "$ref": "#/$defs/documentationGroup"
      },
      "minItems": 1
    },
    "$extensions": {
      "$ref": "common/extensions.schema.json#/$defs/extensions"
    }
  },
  "additionalProperties": false,
  "$defs": {
    "documentationGroup": {
      "type": "object",
      "description": "A named collection of design system documentation entities. Contains a single `items` array of typed entities — components, tokens, token groups, themes, styles, and/or patterns mixed freely in any order. Each entity is identified by its `type` discriminator property. At least one entity must be present.",
      "required": ["name", "items"],
      "properties": {
        "name": {
          "type": "string",
          "description": "Human-readable name of the collection (e.g., 'Acme Design System', 'Color Tokens', 'Button Documentation')."
        },
        "description": {
          "$ref": "common/rich-text.schema.json#/$defs/richText"
        },
        "items": {
          "type": "array",
          "description": "The entities in this documentation group. Each item is a typed entity — its `type` property determines which schema applies. Entities of different types can be mixed freely in the same array. The ordering is significant: tools SHOULD preserve it for display. Authors MAY organize entities in whatever order best serves their documentation (e.g., tokens before components, or grouped by domain).",
          "items": {
            "oneOf": [
              {
                "$ref": "entities/component.schema.json#/$defs/component"
              },
              {
                "$ref": "entities/token.schema.json#/$defs/token"
              },
              {
                "$ref": "entities/token.schema.json#/$defs/tokenGroup"
              },
              {
                "$ref": "entities/theme.schema.json#/$defs/theme"
              },
              {
                "$ref": "entities/style.schema.json#/$defs/style"
              },
              {
                "$ref": "entities/pattern.schema.json#/$defs/pattern"
              }
            ]
          },
          "minItems": 1
        },
        "$extensions": {
          "$ref": "common/extensions.schema.json#/$defs/extensions"
        }
      },
      "additionalProperties": false
    }
  }
}
