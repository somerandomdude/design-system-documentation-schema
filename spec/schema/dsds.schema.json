{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://designsystemdocspec.org/v0.6/dsds.schema.json",
  "title": "Design System Documentation Spec (DSDS) v0.6",
  "description": "A machine-readable format for design system documentation covering components, design tokens, token groups, themes, foundations, patterns, and guides. A DSDS document contains one or more documentation groups — named collections of typed entities. Each entity carries a `kind` discriminator identifying it as a component, token, token-group, theme, foundation, pattern, or guide. Entity schemas are in entities/. Document block schemas are in document-blocks/. Common schemas are in common/.",
  "type": "object",
  "required": ["dsdsVersion"],
  "oneOf": [
    { "required": ["documentation"] },
    { "required": ["entity"] }
  ],
  "properties": {
    "$schema": {
      "type": "string",
      "description": "URI reference to the DSDS JSON Schema for validation."
    },
    "dsdsVersion": {
      "type": "string",
      "const": "0.6",
      "description": "The DSDS spec version this document conforms to."
    },
    "systemInfo": {
      "$ref": "common/system-info.schema.json#/$defs/systemInfo"
    },
    "extends": {
      "$ref": "common/extends.schema.json#/$defs/documentExtends",
      "description": "Declares that this DSDS document inherits from another DSDS document — typically a core design system. The base document provides the foundational entities. This document layers on top, adding or extending entities. Enables systems-of-systems architectures where product- or brand-specific systems inherit from a shared core."
    },
    "documentBlocks": {
      "type": "array",
      "description": "System-level structured documentation for the design system as a whole — its purpose (when to adopt the system), cross-cutting guidelines, and narrative overviews. Each entry is a general document block: a `purpose`, `guideline`, `section`, `accessibility`, or `content` block. This is the same `documentBlocks` mechanism entities use, scoped to the system rather than a single artifact; scope is signalled by position (root vs. inside an entity).",
      "items": {
        "$ref": "document-blocks/document-blocks.schema.json#/$defs/generalDocumentBlock"
      }
    },
    "documentation": {
      "type": "array",
      "description": "One or more documentation groups. Each group is a named collection that organizes entities into separate typed arrays — components, tokenGroups, themes, foundations, patterns, and guides. Multiple groups let a single DSDS file organize entities into logical sections (e.g., one group for foundations, another for components). All entity arrays are optional; a group only needs a name. Items may be inlined or referenced via a $ref object pointing to another DSDS file.",
      "items": {
        "oneOf": [
          { "$ref": "#/$defs/documentationGroup" },
          { "$ref": "#/$defs/fileRef" }
        ]
      },
      "minItems": 1
    },
    "entity": {
      "description": "A single entity document — one component, token, token group, theme, foundation, pattern, or guide. Use this instead of `documentation` when each entity lives in its own file. The `kind` field on the entity identifies its type.",
      "oneOf": [
        { "$ref": "entities/component.schema.json#/$defs/component" },
        { "$ref": "entities/pattern.schema.json#/$defs/pattern" },
        { "$ref": "entities/foundation.schema.json#/$defs/foundation" },
        { "$ref": "entities/theme.schema.json#/$defs/theme" },
        { "$ref": "entities/token.schema.json#/$defs/token" },
        { "$ref": "entities/token.schema.json#/$defs/tokenGroup" },
        { "$ref": "entities/guide.schema.json#/$defs/guide" }
      ]
    },
    "$extensions": {
      "$ref": "common/extensions.schema.json#/$defs/extensions"
    }
  },
  "additionalProperties": false,
  "$defs": {
    "fileRef": {
      "type": "object",
      "description": "A JSON Reference to a DSDS entity or documentation group defined in another file. Tooling (e.g. json-schema-ref-parser) resolves the reference before validation and merge. The URI should include a JSON Pointer fragment to target a specific value within the referenced file (e.g. './button.dsds.json#/entity', './groups/tokens.dsds.json#/documentation/0').",
      "required": ["$ref"],
      "properties": {
        "$ref": {
          "type": "string",
          "description": "A relative reference to a DSDS file, optionally with a JSON Pointer fragment identifying the target value within that file (e.g., './button.dsds.json#/entity'). MUST be relative: absolute paths, protocol-relative references, and scheme-bearing URIs (http:, file:, …) are not permitted — a reference to another system uses `extends.url`. Resolvers MUST detect cycles, treat unresolvable files/pointers and shape mismatches as fatal, and gate any remote dereferencing behind an allow-list (see the spec's reference-resolution and security rules)."
        }
      },
      "additionalProperties": false
    },
    "documentationGroup": {
      "type": "object",
      "description": "A named collection of design system documentation entities. Entities are organized into separate typed arrays — components, tokenGroups, themes, foundations, patterns, and guides — rather than a single mixed array. Only the group name is required; all entity arrays are optional, so a group may contain any combination of entity types.",
      "required": ["name"],
      "properties": {
        "name": {
          "type": "string",
          "description": "Human-readable name of the collection (e.g., 'Acme Design System', 'Color Tokens', 'Button Documentation'). This is already a display label — it does NOT have an `identifier` counterpart and need not match a slug pattern."
        },
        "description": {
          "$ref": "common/rich-text.schema.json#/$defs/richText"
        },
        "components": {
          "type": "array",
          "description": "Component entities in this documentation group. Each item describes a UI component — its anatomy, variants, states, accessibility guidelines, and usage rules. Items may be inlined or referenced via a $ref object pointing to another DSDS file.",
          "items": {
            "oneOf": [
              { "$ref": "entities/component.schema.json#/$defs/component" },
              { "$ref": "#/$defs/fileRef" }
            ]
          }
        },
        "tokenGroups": {
          "type": "array",
          "description": "Token group entities in this documentation group. Each item describes a collection of related design tokens — colors, spacing, typography scales, etc. Items may be inlined or referenced via a $ref object pointing to another DSDS file.",
          "items": {
            "oneOf": [
              { "$ref": "entities/token.schema.json#/$defs/tokenGroup" },
              { "$ref": "#/$defs/fileRef" }
            ]
          }
        },
        "themes": {
          "type": "array",
          "description": "Theme entities in this documentation group. Each item describes a theme — a named set of token overrides that customize the visual appearance of the system (e.g., light/dark mode, brand variations). Items may be inlined or referenced via a $ref object pointing to another DSDS file.",
          "items": {
            "oneOf": [
              { "$ref": "entities/theme.schema.json#/$defs/theme" },
              { "$ref": "#/$defs/fileRef" }
            ]
          }
        },
        "foundations": {
          "type": "array",
          "description": "Foundation entities in this documentation group. Each item documents a broad, cross-cutting design domain — color, typography, spacing, elevation, motion, shape, accessibility, content, and the like — with its principles, scales, and guidelines. Items may be inlined or referenced via a $ref object pointing to another DSDS file.",
          "items": {
            "oneOf": [
              { "$ref": "entities/foundation.schema.json#/$defs/foundation" },
              { "$ref": "#/$defs/fileRef" }
            ]
          }
        },
        "patterns": {
          "type": "array",
          "description": "Pattern entities in this documentation group. Each item describes a UX pattern — a reusable solution to a common design problem, composed of one or more components (e.g., form validation, empty states, navigation flows). Items may be inlined or referenced via a $ref object pointing to another DSDS file.",
          "items": {
            "oneOf": [
              { "$ref": "entities/pattern.schema.json#/$defs/pattern" },
              { "$ref": "#/$defs/fileRef" }
            ]
          }
        },
        "guides": {
          "type": "array",
          "description": "Guide entities in this documentation group. Each item is a long-form, reading-oriented document — a getting-started walkthrough, a contribution guide, a tutorial, a conceptual overview, or a migration guide. Items may be inlined or referenced via a $ref object pointing to another DSDS file.",
          "items": {
            "oneOf": [
              { "$ref": "entities/guide.schema.json#/$defs/guide" },
              { "$ref": "#/$defs/fileRef" }
            ]
          }
        },
        "$extensions": {
          "$ref": "common/extensions.schema.json#/$defs/extensions"
        }
      },
      "additionalProperties": false
    }
  }
}
