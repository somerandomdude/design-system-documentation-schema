{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://designsystemdocspec.org/v0.1/dsds.schema.json",
  "title": "Design System Documentation Standard (DSDS) v0.1",
  "description": "A machine-readable format for design system documentation covering components, design tokens, token groups, themes, styles, and patterns. A DSDS document contains one or more documentation groups — named collections of typed entities. Each entity carries a `kind` discriminator identifying it as a component, token, token-group, theme, style, or pattern. Entity schemas are in entities/. Document block schemas are in document-blocks/. Common schemas are in common/.",
  "type": "object",
  "required": ["dsdsVersion", "documentation"],
  "properties": {
    "$schema": {
      "type": "string",
      "description": "URI reference to the DSDS JSON Schema for validation."
    },
    "dsdsVersion": {
      "type": "string",
      "const": "0.1",
      "description": "The DSDS spec version this document conforms to."
    },
    "metadata": {
      "$ref": "common/metadata.schema.json#/$defs/metadata"
    },
    "extends": {
      "$ref": "common/extends.schema.json#/$defs/documentExtends",
      "description": "Declares that this DSDS document inherits from another DSDS document — typically a core design system. The base document provides the foundational entities. This document layers on top, adding or extending entities. Enables systems-of-systems architectures where product- or brand-specific systems inherit from a shared core."
    },
    "purpose": {
      "type": "object",
      "description": "The purpose of the design system as a whole — why it exists, what problems it solves, and who it serves. Helps stakeholders, new team members, and external consumers understand the system's intent before exploring individual components or tokens.",
      "properties": {
        "description": {
          "$ref": "common/rich-text.schema.json#/$defs/richText",
          "description": "A high-level description of the design system's purpose and the problems it solves."
        },
        "useCases": {
          "type": "array",
          "description": "Scenario-driven guidance describing when this design system is or is not the right choice. Positive cases describe when the system should be adopted. Negative cases describe when a different approach fits better, and why.",
          "items": {
            "$ref": "common/purpose.schema.json#/$defs/useCase"
          }
        }
      },
      "additionalProperties": false
    },
    "bestPractices": {
      "type": "array",
      "description": "System-level best practices that apply across the entire design system — broad guidance for teams adopting, contributing to, or consuming the system. These are not component-specific rules (those live in each entity's guidelines array) but cross-cutting principles like 'always use semantic tokens instead of raw values' or 'test all components at 200% zoom'. Each entry is a self-contained rule paired with a rationale.",
      "items": {
        "$ref": "document-blocks/guideline.schema.json#/$defs/guidelineEntry"
      }
    },
    "documentation": {
      "type": "array",
      "description": "One or more documentation groups. Each group is a named collection that organizes entities into separate typed arrays — components, tokenGroups, themes, styles, and patterns. Multiple groups let a single DSDS file organize entities into logical sections (e.g., one group for foundations, another for components). All entity arrays are optional; a group only needs a name.",
      "items": {
        "$ref": "#/$defs/documentationGroup"
      },
      "minItems": 1
    },
    "$extensions": {
      "$ref": "common/extensions.schema.json#/$defs/extensions"
    }
  },
  "additionalProperties": false,
  "$defs": {
    "documentationGroup": {
      "type": "object",
      "description": "A named collection of design system documentation entities. Entities are organized into separate typed arrays — components, tokenGroups, themes, styles, and patterns — rather than a single mixed array. Only the group name is required; all entity arrays are optional, so a group may contain any combination of entity types.",
      "required": ["name"],
      "properties": {
        "name": {
          "type": "string",
          "description": "Human-readable name of the collection (e.g., 'Acme Design System', 'Color Tokens', 'Button Documentation')."
        },
        "description": {
          "$ref": "common/rich-text.schema.json#/$defs/richText"
        },
        "components": {
          "type": "array",
          "description": "Component entities in this documentation group. Each item describes a UI component — its anatomy, variants, states, accessibility guidelines, and usage rules.",
          "items": {
            "$ref": "entities/component.schema.json#/$defs/component"
          }
        },
        "tokenGroups": {
          "type": "array",
          "description": "Token group entities in this documentation group. Each item describes a collection of related design tokens — colors, spacing, typography scales, etc.",
          "items": {
            "$ref": "entities/token.schema.json#/$defs/tokenGroup"
          }
        },
        "themes": {
          "type": "array",
          "description": "Theme entities in this documentation group. Each item describes a theme — a named set of token overrides that customize the visual appearance of the system (e.g., light/dark mode, brand variations).",
          "items": {
            "$ref": "entities/theme.schema.json#/$defs/theme"
          }
        },
        "styles": {
          "type": "array",
          "description": "Style entities in this documentation group. Each item describes a reusable visual style — a named combination of token values that can be applied to elements (e.g., elevation levels, text styles).",
          "items": {
            "$ref": "entities/style.schema.json#/$defs/style"
          }
        },
        "patterns": {
          "type": "array",
          "description": "Pattern entities in this documentation group. Each item describes a UX pattern — a reusable solution to a common design problem, composed of one or more components (e.g., form validation, empty states, navigation flows).",
          "items": {
            "$ref": "entities/pattern.schema.json#/$defs/pattern"
          }
        },
        "$extensions": {
          "$ref": "common/extensions.schema.json#/$defs/extensions"
        }
      },
      "additionalProperties": false
    }
  }
}
