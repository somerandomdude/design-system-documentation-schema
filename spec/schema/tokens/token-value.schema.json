{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://designsystemdocspec.org/v0.1/token-value.schema.json",
  "title": "DSDS Token Value Definition",
  "description": "A displayable representation of a design token's value for documentation tools. Captures the resolved value, any alias reference, and an optional link to the authoritative DTCG file.",
  "$defs": {
    "tokenValue": {
      "type": "object",
      "description": "A displayable representation of the token's value for documentation tools.",
      "properties": {
        "resolved": {
          "type": "string",
          "description": "The resolved value as a human-readable string (e.g., '#1a1a1a', '16px', '400', 'rgb(0, 85, 179)')."
        },
        "reference": {
          "type": "string",
          "description": "If this token is an alias, the name of the referenced token (e.g., 'color.neutral.900', 'spacing.base')."
        },
        "dtcgFile": {
          "type": "string",
          "description": "URI or relative path to the W3C Design Tokens Format (DTCG) file containing the authoritative value."
        }
      },
      "additionalProperties": false
    }
  }
}
