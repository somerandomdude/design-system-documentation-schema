{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://designsystemdocspec.org/v0.1/token.schema.json",
  "title": "DSDS Token Definition",
  "description": "Documentation for a single design token — its name, type, value, platform API mappings, use cases, guidelines, and accessibility considerations.",
  "$defs": {
    "tokenDoc": {
      "type": "object",
      "description": "Documentation for a single design token.",
      "required": ["name", "displayName", "description", "status", "tokenType"],
      "properties": {
        "name": {
          "type": "string",
          "description": "The token name. Token names are intentionally unconstrained by a pattern to accommodate DTCG and design tool naming conventions that use dots, slashes, or other separators (e.g., 'color-text-primary', 'color.text.primary', 'color/text/primary')."
        },
        "displayName": {
          "type": "string",
          "description": "Human-readable name."
        },
        "summary": {
          "type": "string",
          "description": "A one-line, plain-text summary. MUST NOT contain markup."
        },
        "description": {
          "$ref": "../common/rich-text.schema.json#/$defs/richText"
        },
        "status": {
          "$ref": "../common/status.schema.json#/$defs/status"
        },
        "deprecationNotice": {
          "type": "string",
          "description": "Required when status is 'deprecated'."
        },
        "since": {
          "type": "string",
          "description": "The version in which this token was introduced."
        },
        "tags": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Freeform keywords to relate guidelines across categories/disciplines (e.g., 'rtl', 'localization', 'validation')."
        },
        "tokenType": {
          "type": "string",
          "description": "The type of token aligned with the DTCG spec (e.g., 'color', 'dimension', 'fontFamily', 'fontWeight', 'duration', 'cubicBezier', 'shadow')."
        },
        "category": {
          "type": "string",
          "description": "The token's semantic category. Standard values: 'base' (raw palette values, not used directly in components), 'semantic' (purpose-driven tokens that map to base values, e.g., 'color-text-primary'), 'component' (tokens scoped to a specific component, e.g., 'button-background'). Custom values are permitted and SHOULD be lowercase kebab-case."
        },
        "value": {
          "$ref": "token-value.schema.json#/$defs/tokenValue"
        },
        "aliases": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Names of tokens that alias this token."
        },
        "api": {
          "$ref": "token-api.schema.json#/$defs/tokenApi"
        },
        "useCases": {
          "$ref": "../common/usecase.schema.json#/$defs/useCases"
        },
        "guidelines": {
          "type": "array",
          "description": "Usage guidelines for the token. Each entry is a self-contained guideline. Use the `category` property on individual guidelines to group by concern (e.g., 'design', 'accessibility', 'content').",
          "items": {
            "$ref": "../common/guideline.schema.json#/$defs/guideline"
          }
        },
        "accessibility": {
          "$ref": "../common/accessibility.schema.json#/$defs/accessibilityObject"
        },
        "links": {
          "type": "array",
          "description": "External resource links and related artifact references.",
          "items": {
            "$ref": "../common/link.schema.json#/$defs/link"
          }
        },
        "$extensions": {
          "$ref": "../common/extensions.schema.json#/$defs/extensions"
        }
      },
      "if": {
        "properties": {
          "status": {
            "const": "deprecated"
          }
        }
      },
      "then": {
        "required": ["deprecationNotice"]
      },
      "additionalProperties": false
    }
  }
}
