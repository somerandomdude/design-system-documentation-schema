{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://designsystemdocspec.org/v0.1/guidelines/purpose.schema.json",
  "title": "DSDS Purpose Guideline",
  "description": "Documents what an artifact is used for — the concrete scenarios in which it is the right choice and the scenarios in which a different artifact would be more appropriate. The purpose guideline wraps the common useCases data model with a guideline `type` discriminator so it can appear in the unified guidelines array on any entity. This follows the same pattern used by variant values and state entries, where a `useCases` property holds the whenToUse / whenNotToUse arrays.",
  "$defs": {
    "purpose": {
      "type": "object",
      "description": "Documents the purpose of an artifact — when to reach for it and when to choose something else. Contains a `useCases` object with whenToUse and whenNotToUse arrays of scenario-driven use case entries drawn from the common useCases schema. This follows the same pattern used by variant values and state entries. Purpose guidelines help the reader decide whether this is the right artifact *before* they start implementing it. For guidance on *how* to use the artifact correctly after choosing it, see best-practices. For guidance on *what* the artifact looks like and how it behaves, see variants, states, and anatomy.",
      "required": ["kind"],
      "properties": {
        "kind": {
          "type": "string",
          "const": "purpose",
          "description": "Identifies this guideline as a purpose specification."
        },
        "useCases": {
          "type": "array",
          "description": "Scenario-driven guidance describing when this artifact is or is not the right choice. ",
          "items": {
            "$ref": "../common/usecase.schema.json#/$defs/useCase"
          }
        }
      },
      "additionalProperties": false
    }
  }
}
