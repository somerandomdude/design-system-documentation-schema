{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://designsystemdocspec.org/v0.1/style.schema.json",
  "title": "DSDS Style Definitions",
  "description": "Definitions for documenting macro-level visual style guidelines — color palettes, typography systems, spacing scales, elevation levels, and other visual attributes. Style documentation provides the rationale, principles, and usage rules that govern how token values are applied across the system.",
  "$defs": {
    "principle": {
      "type": "object",
      "description": "A high-level guiding principle for the visual style.",
      "required": [
        "title",
        "description"
      ],
      "properties": {
        "title": {
          "type": "string",
          "description": "A short name for the principle."
        },
        "description": {
          "$ref": "../common/rich-text.schema.json#/$defs/richText"
        }
      },
      "additionalProperties": false
    },
    "scaleStep": {
      "type": "object",
      "description": "A single step in an ordered scale (e.g., a type size, a spacing value, an elevation level).",
      "required": [
        "token"
      ],
      "properties": {
        "token": {
          "type": "string",
          "description": "The token name for this step. MUST reference a token defined in the token schema."
        },
        "label": {
          "type": "string",
          "description": "A human-readable label (e.g., 'sm', 'md', 'lg', '2xl')."
        },
        "value": {
          "type": "string",
          "description": "The resolved value for display (e.g., '16px', '#1a1a1a', '400')."
        },
        "description": {
          "$ref": "../common/rich-text.schema.json#/$defs/richText"
        }
      },
      "additionalProperties": false
    },
    "scale": {
      "type": "object",
      "description": "An ordered sequence of values that forms a visual scale (e.g., type scale, spacing scale, elevation scale). Each step references a design token.",
      "required": [
        "name",
        "description",
        "steps"
      ],
      "properties": {
        "name": {
          "type": "string",
          "description": "Machine-readable name of the scale."
        },
        "displayName": {
          "type": "string",
          "description": "Human-readable name."
        },
        "description": {
          "$ref": "../common/rich-text.schema.json#/$defs/richText"
        },
        "steps": {
          "type": "array",
          "description": "The ordered steps in the scale, from smallest/lowest to largest/highest.",
          "items": {
            "$ref": "#/$defs/scaleStep"
          },
          "minItems": 1
        }
      },
      "additionalProperties": false
    },
    "styleDoc": {
      "type": "object",
      "description": "Documentation for a macro-level visual style — the principles, guidelines, scales, and token references that govern a visual attribute such as color, typography, spacing, or elevation across the entire system.",
      "required": [
        "name",
        "displayName",
        "description",
        "status",
        "category"
      ],
      "properties": {
        "name": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9-]*$",
          "description": "Machine-readable identifier (e.g., 'color', 'typography', 'spacing', 'elevation')."
        },
        "displayName": {
          "type": "string",
          "description": "Human-readable name."
        },
        "summary": {
          "type": "string",
          "description": "A one-line, plain-text summary. MUST NOT contain markup."
        },
        "description": {
          "$ref": "../common/rich-text.schema.json#/$defs/richText"
        },
        "status": {
          "$ref": "../common/status.schema.json#/$defs/status"
        },
        "deprecationNotice": {
          "type": "string",
          "description": "Required when status is 'deprecated'."
        },
        "since": {
          "type": "string",
          "description": "The version in which this style was introduced."
        },
        "tags": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Freeform keywords to relate guidelines across categories/disciplines (e.g., 'rtl', 'localization', 'validation')."
        },
        "category": {
          "type": "string",
          "description": "The visual attribute category (e.g., 'color', 'typography', 'spacing', 'elevation', 'motion', 'shape')."
        },
        "principles": {
          "type": "array",
          "description": "High-level guiding principles for this visual style.",
          "items": {
            "$ref": "#/$defs/principle"
          }
        },
        "tokenGroups": {
          "type": "array",
          "description": "References to the token groups that implement this style. Connects macro-level guidance to concrete token values defined in the token schema.",
          "items": {
            "$ref": "../common/artifact-reference.schema.json#/$defs/artifactReference"
          }
        },
        "scales": {
          "type": "array",
          "description": "Ordered scales that define the progression of values for this style (e.g., a type scale, a spacing scale, an elevation scale). Each scale step references a design token.",
          "items": {
            "$ref": "#/$defs/scale"
          }
        },
        "useCases": {
          "$ref": "../common/usecase.schema.json#/$defs/useCases"
        },
        "guidelines": {
          "type": "array",
          "description": "Usage guidelines for the style. Each entry is a self-contained guideline. Use the `category` property on individual guidelines to group by concern (e.g., 'design', 'accessibility', 'content').",
          "items": {
            "$ref": "../common/guideline.schema.json#/$defs/guideline"
          }
        },
        "accessibility": {
          "$ref": "../common/accessibility.schema.json#/$defs/accessibilityObject"
        },
        "links": {
          "type": "array",
          "description": "External resource links and related artifact references.",
          "items": {
            "$ref": "../common/link.schema.json#/$defs/link"
          }
        },
        "$extensions": {
          "$ref": "../common/extensions.schema.json#/$defs/extensions"
        }
      },
      "if": {
        "properties": {
          "status": {
            "const": "deprecated"
          }
        }
      },
      "then": {
        "required": [
          "deprecationNotice"
        ]
      },
      "additionalProperties": false
    }
  }
}
