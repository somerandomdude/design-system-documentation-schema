id: back-to-top
kind: component
name: BackToTop
description: "A floating link back to the top of the page, shown once the page has scrolled past its start."

metadata:
  status: {status: stable}

sourceFiles:
  - platform: web-component
    file: ../../../site/components/back-to-top.js
