id: logo
kind: component
name: Logo
description: "The DSDS mark, inlined from its source SVG so its fill can be recolored at runtime via the current text color."

metadata:
  status: {status: stable}

sourceFiles:
  - platform: web-component
    file: ../../../site/components/logo.js
